# Base URL of the backend API. NEVER hardcode localhost inside components —
# always read it from this env var so switching environments (staging, prod)
# requires zero code changes.
VITE_API_BASE_URL=http://localhost:8000
